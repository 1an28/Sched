import React from "react";

const Form: React.FC = () => {

    return(
        <section>
            <input type = "time" />
            <input type = "time" />
        </section>
    );
};

export default Form;